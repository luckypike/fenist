.form
  = simple_form_for([:admin, @speaker]) do |f|
    = f.error_notification

    .form_inputs
      = f.input :title
      = f.input :photo
      = f.input :desc

    .form_actions
      = f.button :submit
